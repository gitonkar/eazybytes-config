server:
  port: 8080
spring:
  application:
    name: "accounts"
  profiles:
    active:
      - "qa"
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"
  datasource:
    url: jdbc:mysql://localhost:3306/school_db
    username: root
    password: root  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  sql:
    init:
      mode: always

management:
  endpoints:
    web:
      exposure:
        include: "*"      

build:
  version: "3.0"

accounts:
  message: "Welcome to EazyBank accounts related docker APIs with hookdeck added"
  contactDetails:
    name: "John Doe - Developer"
    email: "john@eazybank.com"
  onCallSupport:
    - (555) 555-1234
    - (555) 523-1345
